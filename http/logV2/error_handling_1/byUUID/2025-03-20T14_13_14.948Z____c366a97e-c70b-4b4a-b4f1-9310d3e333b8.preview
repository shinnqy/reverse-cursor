{"line":0,"column":14}

-------------------------------[        currentFileContentsWithToFill        ]--------------------------------
function calcu[ToFill]lateSum(a: number, b: number {
  return a + b;
}

const user = {
  name: "Alice"
  age: 30
};

if (user.age > 25 && (user.name === "Alice" {
}

const message = `Hello, ${user.name;

const list = [1, 2, 3;

function printUser(user: { name: string }) {
  console.log(user.name);
}

interface Response<T> { data: T }
let res: Response<string> = { data: "OK" };
let value = res.dat;

-------------------------------[               firstChunkValue               ]--------------------------------
function calculateSum(a: number, b: number) {
  return a + b;
}

-------------------------------[       replacedContentsWithFirstChunk        ]--------------------------------
function calculateSum(a: number, b: number) {
  return a + b;
}

const user = {
  name: "Alice"
  age: 30
};

if (user.age > 25 && (user.name === "Alice" {
}

const message = `Hello, ${user.name;

const list = [1, 2, 3;

function printUser(user: { name: string }) {
  console.log(user.name);
}

interface Response<T> { data: T }
let res: Response<string> = { data: "OK" };
let value = res.dat;

-------------------------------[             fusedCursorPrediction            ]--------------------------------
{
  "relativePath": "entry/src/main/ets/utils/errorHandling.ets",
  "lineNumberOneIndexed": 10,
  "text": "if (user.age > 25 && (user.name === \"Alice\" {\n}\n\nconst message = `Hello, ${user.name;\n",
  "shouldRetriggerCpp": true
}
--------------------------------[        displayedFusedCursorPrediction       ]---------------------------------
{
  "relativePath": "entry/src/main/ets/utils/errorHandling.ets",
  "lineNumberOneIndexed": 10,
  "text": "if (user.age > 25 && (user.name === \"Alice\" {\n}\n\nconst message = `Hello, ${user.name;\n",
  "shouldRetriggerCpp": true
}