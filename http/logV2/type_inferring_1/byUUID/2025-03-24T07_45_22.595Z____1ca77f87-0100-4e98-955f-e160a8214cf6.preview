{"line":18,"column":14}

-------------------------------[        currentFileContentsWithToFill        ]--------------------------------
interface User {
    id: number;
    name: string;
    age: number;
}
interface Admin { level: number; permission: string[] }

interface Post {title: string; content: string}

interface Comment {
    id: number;
    author: string;
}

type UserRole = User | Admin;

type Content = Post | Comment;

type Result<>T[ToFill]

-------------------------------[               firstChunkValue               ]--------------------------------

type Result<T> = { success: true; data: T } | { success: false; error: string };

-------------------------------[                   fullText                  ]--------------------------------

type Result<T> = { success: true; data: T } | { success: false; error: string };

function getUserInfo(id: number): Result<User> {
    return { success: true, data: { id, name: 'John', age: 20 } };
}


