{"line":36,"column":23}

-------------------------------[        currentFileContentsWithToFill        ]--------------------------------
function greet(name: string) {
    return `Hello, ${name}!`;
}

greet('John');
greet('Jane');

interface Person {
    name: string;
    age: number;
}

function printPerson(person: Person) {
    console.log(person);
}

printPerson({ name: 'John', age: 20 });
printPerson({ name: 'Jane', age: 21 });


function printValue(value: string | number) {
    console.log(value);
}

printValue('Hello');
printValue(123);

function createUser(name: string, age: number) {
    return {
        name,
        age,
    };
}

const user = createUser('John', 20);

function updateConfig({[ToFill]})

-------------------------------[               firstChunkValue               ]--------------------------------

const user = createUser('John', 20);

function updateConfig({ url, method, data }: { url: string, method: string, data: any }) {
    console.log(url, method, data);
}

updateConfig({ url: 'https://example.com', method: 'GET', data: { name: 'John', age: 20 } });


