{"line":21,"column":35}

-------------------------------[        currentFileContentsWithToFill        ]--------------------------------
function add(a: number, b: number): number {
    return a + b;
}

function subtract(a: number, b: number): number {
    return a - b;
}

let sum = add(1, 2);

interface User {
    name: string;
    age: number;
}

function getUser(name: string, age: number): User {
    return { name, age };
}

let user = getUser("John", 30);

function wrapValue<T>(value: T): {d[ToFill] {

-------------------------------[               firstChunkValue               ]--------------------------------
}

let user = getUser("John", 30);

function wrapValue<T>(value: T): { getValue: () => T } {
    return {
        getValue: () => value
    };
}

let wrappedValue = wrapValue(10);
